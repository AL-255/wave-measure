Operators
=========

The streaming operators applied by the :doc:`categories`. Most users reach these
through ``wave.filter`` / ``wave.amplitude`` / ``wave.math`` rather than directly.

.. automodule:: wave_measure.operators
   :members:
