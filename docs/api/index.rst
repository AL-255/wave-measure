API reference
=============

Everything documented here is re-exported from the top-level ``wave_measure``
package (imported as ``wm``), so ``wm.Waveform``, ``wm.read_raw``, ``wm.render``,
and so on all work.

.. toctree::
   :maxdepth: 2

   waveform
   sources
   io
   categories
   operators
   reductions
   measurements
   analysis
   rendering
   accelerator
